{
  "name": "afiliado-functions",
  "description": "Cloud Functions para webhook Kiwify (carrinho abandonado)",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.5.0",
    "stripe": "^22.3.1",
    "web-push": "^3.6.7"
  }
}
